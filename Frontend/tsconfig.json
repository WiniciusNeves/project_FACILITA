{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "types": ["react-native-dotenv"], // Adiciona suporte ao @env
    "allowSyntheticDefaultImports": true // Exemplo de outra configuração
  }
}